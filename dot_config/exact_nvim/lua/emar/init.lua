-- "emar.pack" needs to go first since this is what add the plugins that can be 
-- used by anything going after this 
require("emar.pack")
require("emar.set")
require("emar.globals")
require("emar.remap")
